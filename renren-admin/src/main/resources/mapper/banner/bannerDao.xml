<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bannerDao">
    <select id="getBanner" resultType="io.renren.common.entity.PageData">
        SELECT
        banner.`id`,banner.`type`,filetable.`filepath`
        FROM banner
        LEFT JOIN filetable ON filetable.`id` = banner.`imageid`
    </select>

</mapper>