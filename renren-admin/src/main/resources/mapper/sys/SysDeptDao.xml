<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.modules.sys.dao.SysDeptDao">

    <select resultType="io.renren.common.entity.PageData" id="deptlistPage">
        SELECT d.`name`,d.`order_num`,d.`parent_id`,d.`dept_id`
        FROM sys_dept d
        WHERE d.`del_flag`=0
        AND d.`corid`= #{pd.corId,jdbcType=INTEGER}
        <if test="pd.deptName != null">
            AND d.`name` LIKE CONCAT('%',#{pd.deptName,jdbcType=VARCHAR},'%')
        </if>
    </select>

    <select id="selectDeptById" resultType="io.renren.common.entity.PageData">
        SELECT d.`name`,d.`order_num`,d.`parent_id`,d.`corid`,d.`dept_id`
        FROM sys_dept d
        WHERE d.`del_flag`=0
        AND d.`dept_id`= #{deptId,jdbcType=INTEGER}
    </select>

    <insert id="save">
        INSERT INTO sys_dept(`parent_id`,`name`,`corid`)
        VALUES (#{parentId,jdbcType=INTEGER},#{name,jdbcType=VARCHAR},#{corId,jdbcType=INTEGER})
    </insert>

    <update id="update">
        UPDATE sys_dept
        <set>
            <if test="parentId!=null">
                parent_id=#{parentId,jdbcType=INTEGER},
            </if>
            <if test="name!=null">
                `name`=#{name,jdbcType=VARCHAR},
            </if>
            <if test="orderNum!=null">
                `order_num`=#{orderNum,jdbcType=INTEGER},
            </if>
            <if test="delFlag!=null">
                `del_flag`=#{delFlag,jdbcType=INTEGER},
            </if>
            <if test="corId!=null">
                `corid`=#{corId,jdbcType=INTEGER}
            </if>
        </set>
        WHERE  dept_id = #{deptId,jdbcType=INTEGER}
    </update>

    <select id="queryList" resultType="io.renren.modules.sys.entity.SysDeptEntity">
        select t1.*,(select t2.name from sys_dept t2 where t2.dept_id=t1.parent_id)parentName from sys_dept t1 where
        t1.del_flag = 0
        <if test="sql_filter != null">
            and ${sql_filter}
        </if>
    </select>

    <select id="queryDetpIdList" resultType="long">
		select dept_id from sys_dept where parent_id = #{value} and del_flag = 0
	</select>

    <select id="selectAllDept" resultType="io.renren.common.entity.PageData">
        SELECT dept_id,`name`,`parent_id` FROM sys_dept WHERE corid = #{corid} AND d.`del_flag`= 0
    </select>

</mapper>